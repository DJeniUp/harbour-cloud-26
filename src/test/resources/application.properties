# Test profile overrides.
#
# The sharded store + async pipeline (homework 3) need live Postgres instances, which
# aren't available in plain unit tests. Disabling shard.* keeps that whole slice of the
# context out of @SpringBootTest, so the existing MockMvc tests boot with no database.
shard.enabled=false
